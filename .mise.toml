[tools]
python = "3.12"
uv = "0.10.11"
lefthook = "2.1.10"

[tasks.setup]
description = "プロジェクトの初期セットアップ"
run = ["uv sync", "lefthook install"]

[tasks.sync]
description = "Discord Botのコマンドを同期"
run = "uv run src/main.py --sync-only"

[tasks.dev]
description = "Discord Botの起動"
run = "uv run src/main.py"

[tasks.fmt]
description = "コードフォーマット"
run = "uv run ruff format ."

[tasks.fmt-check]
description = "フォーマットチェック（CI用）"
run = "uv run ruff format . --check"

[tasks.lint]
description = "Lintチェック"
run = "uv run ruff check . --fix"

[tasks.lint-check]
description = "Lintチェック（CI用・自動修正なし）"
run = "uv run ruff check ."

[tasks.test]
description = "テストの実行"
run = "uv run pytest"
